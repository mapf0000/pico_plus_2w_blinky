# Example: repeat + let (Phase 1)

# Define constants
let GREETING = "Hello, world!"
let DELAY_MS = 15

# Repeat the block three times
repeat(3) {
  text(GREETING, DELAY_MS)
  tap("ENTER")
}

# Use numeric let in delay
let PAUSE = 250
delay(PAUSE)
modtap("LCTRL+LALT+DELETE")
